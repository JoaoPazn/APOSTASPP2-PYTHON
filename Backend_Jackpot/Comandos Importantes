# Executar todos esses comandos no PowerShell:

# Para criar um ambiente virtual:
python -m venv venv

# Para abrir o ambiente virtual:
.\venv\Scripts\activate

# Instalar as bibliotecas necessárias dentro do ambiente virutal:
pip install Flask mariadb bcrypt Flask-Cors
